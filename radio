function radio_do() {
	ssh -p21984 $1 '[ -z $(pgrep mpdscribble) ] && mpdscribble --conf .mpd/scribble.conf'
	mplayer -cache 1000 http://$1:22008
	ssh -p21984 $1 'killall -r mpdscribble'
}
radio_do localhost
radio_do memorici.de
